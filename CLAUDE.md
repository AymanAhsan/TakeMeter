# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

TakeMeter is a fine-tuned text classifier that evaluates discourse quality of anime takes from r/anime. It is a Python ML/NLP project.

## Stack

- Python (ML/NLP)
- `.gitignore` includes patterns for `ruff`, `mypy`, `pytest`, `venv`, and `marimo` — use these tools as they are introduced to the project.

## Project Status

Early-stage: `dataset.csv` and `planning.md` exist but are currently empty. No source code or package configuration exists yet. When adding code, establish a `src/` layout or top-level module alongside a `pyproject.toml` (or `requirements.txt`) as the project matures.
